import React from 'react';
import '../../index.css';

const Navigation = () => (
    <nav>
        <a href="#" onClick={() => window.location.href = "/iniciarSesion"}>Iniciar Sesión</a>
    </nav>
);

export default Navigation;
